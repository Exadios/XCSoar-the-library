To install from a clean pull using 'make' (GNU make):

mkdir out
cd out
cmake -G "Unix Makefiles" ../Init.cmake ..
make

To install from a clean pull using 'ninja' (Ninja):
mkdir out
cd out
cmake -G "Ninja" -C ../Init.cmake ..
ninja
